# leads

Polls D1 every 60s, iMessages cj on new submissions, serves dashboard at http://100.66.165.32:7777.

Run manually: `bun run leads.ts` (from this dir). Install as launchd service: `./install.sh`.
