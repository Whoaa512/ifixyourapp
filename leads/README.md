# leads

Polls D1 every 60s, iMessages cj on new submissions, serves dashboard at https://airbook.polydactyl-pinecone.ts.net.

Run manually: `bun run leads.ts` (from this dir). Install as launchd service: `./install.sh`.
